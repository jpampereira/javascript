console.log("Sentença de código")

// Bloco de código
{
    {
        console.log("Olá");
        console.log('Mundo!') // Padrão do curso
    }
}

/* COMENTÁRIOS:
 * 
 * - O JavaScript permite que as sentenças de código sejam divididas
 * múltiplas linhas, porém, essa sentença não pode ser quebrada em
 * qualquer ponto (Ex: não se pode "quebrar" uma string no meio).
 * 
 * - O ponto e vírgula (;) não é obrigatório no final da sentença (é aconselhado)
 * definir um padrão). 
 */