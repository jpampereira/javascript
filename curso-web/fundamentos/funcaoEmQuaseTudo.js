/* - No JS quase tudo é uma função. Até mesmo as classes, usadas
 * para modelar coisas que podem ser instanciadas criando objetos,
 * no fundo são funções.
 * 
 * - A nova versão do EcmaScript (ES6 - 2015) trouxe a possibilidade
 * de você criar classes utilizando a palavra reservada class, mas
 * antes as classes eram criadas usando a estrutura de funções.
 */

console.log(typeof Object)

class Produto {}
console.log(typeof Produto)