// O Objeto Math em JS permite realizar uma série de operações, como potências, raízes, seno, cosseno, etc.

const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)
//const area = Math.PI * (raio ** 2)

console.log(area.toFixed(2))
console.log(typeof Math)